{{- range $key, $service := .Values.services }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $service.name }}
  namespace: {{ $service.namespace }}
  labels:
    app: {{ $service.name }}
spec:
  replicas: {{ $service.replicas }}
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: {{ $service.name }}
  template:
    metadata:
      labels:
        app: {{ $service.name }}
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: {{ $service.name }}
          image: "{{ $.Values.global.ecrRegistry }}:{{ $service.image }}-{{ $service.tag }}"
          imagePullPolicy: {{ $.Values.global.imagePullPolicy }}

          {{- if ne $service.name "gateway-service" }}
          env:
            - name: DB_HOST
              value: {{ $.Values.database.host | quote }}

            - name: DB_PORT
              value: {{ $.Values.database.port | quote }}

            - name: DB_NAME
              value: {{ $.Values.database.name | quote }}

            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: crm-db-secret
                  key: DB_USER

            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: crm-db-secret
                  key: DB_PASSWORD

            - name: SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE
              value: {{ $.Values.database.hikari.maximumPoolSize | quote }}

            - name: SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE
              value: {{ $.Values.database.hikari.minimumIdle | quote }}

            - name: SPRING_DATASOURCE_HIKARI_CONNECTION_TIMEOUT
              value: {{ $.Values.database.hikari.connectionTimeout | quote }}

            - name: SPRING_DATASOURCE_HIKARI_IDLE_TIMEOUT
              value: {{ $.Values.database.hikari.idleTimeout | quote }}

            - name: SPRING_DATASOURCE_HIKARI_MAX_LIFETIME
              value: {{ $.Values.database.hikari.maxLifetime | quote }}
{{- end }}

{{- if eq $service.name "gateway-service" }}
          envFrom:
            - configMapRef:
                name: gateway-config
{{- end }}

          ports:
            - name: http
              containerPort: {{ $service.port }}
              protocol: TCP

          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
              scheme: HTTP
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
              scheme: HTTP
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3

          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
---
{{- end }}
